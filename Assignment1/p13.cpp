#include <iostream>
using namespace std;
int main()
{ 
    int pos, n;
    cout<<"Enter a number:\n";
    cin>>n;
    for(int i=0;i<=pos;i++)
    {
       
        if (n <= 1)
        {
            return n;
            return fib(n - 1) + fib(n - 2);
         }
    return 0;
}